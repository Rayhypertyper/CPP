#include <bits/stdc++.h>

using namespace std;
int main() {
    vector < int > x = {
        1,2,4,5,6
    };


    x.pop_back(); // remove last element

    x.insert(x.begin()+1, 10); // insert 10 to second element
    
    x.push_back(3)

    
    //sort(x.begin(), x.begin() + 5);

//for (int i = 0; i < 1; i++)
    //cout << x[i] << endl;
    
}